<% if object.errors.any? %>
	<section id="errors">
		<header>
			<h2><%= t('.title') %></h2>
			<h3><%= t('.title_about')%></h3>
		</header>
		<ul>
			<% object.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
		</ul>
	</section>
<% end %>